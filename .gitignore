.DS_Store
*.xcuserstate
xcuserdata/
DerivedData/
build/
*.xcworkspace/xcuserdata/
.swiftpm/

# never commit credentials
*.token
secrets.*
.env

# SwiftPM
.build/

# built app bundle
*.app/

# generated icon: drawn by scripts/make-icon.swift at build time
AppIcon.iconset/
Resources/AppIcon.icns

# fablize story state
.fablize/

# scripts/package.sh: per-arch scratch paths and the shipping archive.
# Not covered by `.build/` above, and scripts/release.sh refuses to run on a
# dirty tree — so a local `make package` used to block the next release until
# they were cleaned up by hand.
.build-arm64/
.build-x86_64/
Runway.zip

# make snapshot
island*.png
